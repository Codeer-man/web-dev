const arr = [4, 64, 754, 347];
const arr2 = [4, 46, 34, 235, 64, 4];
//* display last array

// function lastArr(params) {
//   return params[params.length - 1];
// }

//* combine two array

function combine(params) {
  return params.concate(arr2);
}

console.log(combine(arr));
