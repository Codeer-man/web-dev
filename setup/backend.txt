1. npm init -y
2. npm i express mongooes dotenv cors

3. npm i -D  typescript ts-node @types/node @types/express @types/cors


initialize Ts config
npx tsc -init 

to run server
dev: "nodemon --exec ts-node src/server.ts"


sangam

npm init -y  // initate 

npm install express mongoose dotenv  zod

npm install -D typescript ts-node-dev @types/node @types/express

{
  "compilerOptions": {
    "target": "ES2019",
    "module": "commonjs",
    "rootDir": "src",
    "outDir": "dist",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}




    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
