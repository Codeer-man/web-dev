export for mac
set for windows
set PGPASSWORD="admin"

psql -U postgres -d postgres -f file location
psql -U postgres -d database name

psql command line (cl)

SELECT current_database();
SELECT current_user;
SELECT version();

\l
\dt

to quit ctrl-c / \q / exit
